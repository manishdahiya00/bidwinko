<div class="container">
  <%= form_with(model: @bidPlanCategory, url: admin_bid_plan_category_path) do |form| %>
    <% if @bidPlanCategory.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@bidPlanCategory.errors.count, "error") %> prohibited this bid plan category from being saved:</h2>
        <ul>
          <% @bidPlanCategory.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <!-- /.row -->
    <div class="row">
      <div class="col-lg-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            Basic Form Elements
          </div>
          <div class="panel-body">
            <div class="row">
              <div class="col-lg-6">
                <div class="form-group">
                  <label>Title</label>
                  <%= form.text_field :title, class: "form-control", placeholder: "Enter title" %>
                </div>
                <div class="form-group">
                  <label>Status</label>
                  <%= form.select :status, [['Active', true], ['Inactive', false]],{} ,class: 'form-control' %>
                </div>
                <div class="form-group">
                  <%= form.submit "Update", class: "btn btn-primary" %>
                  <%= link_to 'Back', admin_bid_plan_categories_path, class: "btn btn-default" %>
                </div>
              </div>
            </div>
            <!-- /.row (nested) -->
          </div>
          <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
      </div>
      <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
  <% end %>
</div>
